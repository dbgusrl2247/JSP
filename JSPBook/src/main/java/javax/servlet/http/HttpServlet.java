package javax.servlet.http;

public class HttpServlet {
    // Minimal stub to satisfy JSP compilation in the IDE.
    // Do NOT use this in production; the real servlet API should be provided by the server runtime (Tomcat).
}
